import java.util.Arrays;
public class Main{
    public static void main(String []args){
    int[]numbers={1,3,2,4,2,5};
    Arrays.sort(numbers);
    System.out.println("it is an array aftersorting:"+Arrays.toString(numbers));
    }
}
